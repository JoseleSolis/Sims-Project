@model QuestRequirementsViewModel


@using (Html.BeginForm("Requirements", "Quest", FormMethod.Post, new { id = "questSkills" }))
{
    <table class="table table-striped table-bordered table-sm">
        <tr>
            <th>Name</th>
            <th class="text-center"></th>
        </tr>
        @for (int i = 0; i < Model.Skills.Count; i++)
        {
            @Html.HiddenFor(p => p.Skills[i].SkillID)
            @Html.HiddenFor(p => p.Skills[i].Name)
            @Html.HiddenFor(p => p.Skills[i].Description)
            <tr>
                <td>@Model.Skills[i].Name</td>
                <td><input asp-for="RequiredPoints[i]" type="number" min="0" max="10"></td>
            </tr>
        }
    </table>
    @Html.HiddenFor(p => p.Quest.QuestID)
    @Html.HiddenFor(p => p.Quest.Name)
    @Html.HiddenFor(p => p.Quest.Description)
    @Html.HiddenFor(p => p.Quest.Cost)
    @Html.HiddenFor(p => p.Quest.Reward)

    <button type="submit" class="btn btn-primary">Accept</button>
    <a asp-action="Profile" asp-route-id="@Model.Quest.QuestID" class="btn btn-secondary">Cancel</a>
}
